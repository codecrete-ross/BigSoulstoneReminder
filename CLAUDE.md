@AGENTS.md

Claude-specific notes:

- This repository does not use additional `.claude/rules/`; treat `AGENTS.md` as the canonical shared guidance.
- `.claude/settings.local.json` is local-only configuration.
